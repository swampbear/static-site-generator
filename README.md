# static-site-generator

For making static html page using markdown syntax

Uses delimiters and regex to split markdown text and create nodes that are transelated in order to HTML.

Part of boot.devs static site generator course.
